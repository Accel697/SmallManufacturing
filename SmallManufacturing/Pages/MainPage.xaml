<Page x:Class="SmallManufacturing.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SmallManufacturing.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid Background="#FFFFFF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Column="0" Grid.Row="0" x:Name="btnMaterials" Content="Материалы" Click="btnMaterials_Click" Margin="10"/>
        <Button Grid.Column="1" Grid.Row="0" x:Name="btnProducts" Content="Продукты" Click="btnProducts_Click" Margin="10"/>
        <Button Grid.Column="2" Grid.Row="0" x:Name="btnAddOrder" Content="Добавить заказ" Click="btnAddOrder_Click" Margin="10"/>

        <ListView Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" Name="LVOrder" MouseDoubleClick="LVOrder_MouseDoubleClick" d:ItemsSource="{d:SampleData ItemCount=2}" Margin="5">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <Grid>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text=""/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
