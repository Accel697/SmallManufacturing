<Page x:Class="SmallManufacturing.Pages.MaterialPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SmallManufacturing.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MaterialPage">

    <Grid Background="#FFFFFF">
        <ListView Name="LVMaterial" d:ItemsSource="{d:SampleData ItemCount=2}" Margin="5">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <Grid>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding title}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding Unit1.title, StringFormat='Еденица измерения: \{0}'}"/>
                                <TextBlock Text="{Binding min_residue, StringFormat='Минимальный остаток: \{0}'}"/>
                                <TextBlock Text="{Binding quantityInStock, StringFormat='Количество на складе: \{0}'}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
